import { ISeeder } from "@idsck/components/interfaces";

import { I{{ name }}SeederConfigs, I{{ name }}SeederProps } from "./interfaces";

// please change the config and props or remove if you don't need them
// also dont forget to remove this comment

const config: I{{ name }}SeederConfigs = {
    obj: null,
};

const props: I{{ name }}SeederProps = {
    obj: null,
};

// ==================================================

const seeder: ISeeder = {
    tagName: "{{ name }}",
    thumb: "static/images/components/{{ name }}.png",
    props,
    config,
};

export default seeder;
