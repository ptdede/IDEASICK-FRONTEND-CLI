/**
 *
 * {{ name }} Component
 * TODO: add some documentation here!
 *
 */

import { IWithBaseComponentProps } from "@idsck/hoc/withBaseComponent/interfaces";
import React from "react";

import Styled{{ name }} from "./Styled{{ name }}";

class {{ name }} extends React.Component<IWithBaseComponentProps> {
  render() {
     return (
      <Styled{{ name }}>
        {this.props.children}
      </Styled{{ name }}>
    );
  }
}

export default {{ name }};
