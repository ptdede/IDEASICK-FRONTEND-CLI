/**
 *
 * {{ name }} Component
 * TODO: add some documentation here!
 *
 */

import { IComponentRender } from "@idsck/components/interfaces";
import React from "react";

import Styled{{ name }} from "./Styled{{ name }}";

class {{ name }} extends React.Component<IComponentRender> {
  render() {
     return (
      <Styled{{ name }}>
        {this.props.children}
      </Styled{{ name }}>
    );
  }
}

export default {{ name }};
