/**
 *
 * {{ name }} Component
 * TODO: add some documentation here!
 *
 */

{{#if isWithBaseComponent}}
import withBaseComponent from "@idsck/hoc/withBaseComponent/withBaseComponent";
{{/if}}
import React from "react";

import { I{{ name }}Props } from "./interfaces";
import Styled{{ name }} from "./Styled{{ name }}";

class {{ name }} extends React.Component<I{{ name }}Props> {
    render() {
        return (
            <Styled{{ name }}>
                {this.props.children}
            </Styled{{ name }}>
        ) ;
    }
}

{{#if isWithBaseComponent}}
export default withBaseComponent({{ name }});
{{else}}
export default {{ name }};
{{/if}}
